package patient.registration.database;

import patient.registration.model.Session;
import patient.registration.model.SessionType;

import java.util.Date;
import java.util.List;

class SessionDAOTest {

    @org.junit.jupiter.api.Test
    void sessionDAOTest() {
//        SessionDAO sessionDAO = new SessionDAO();
//
//        for (int i = 0; i < 5; i++) {
//            Session session = new Session(0, 3, SessionType.PLAYING, new Date(), 123, 22.5);
//            sessionDAO.add(session);
//        }
//
//        sessionDAO.remove(1);
//        sessionDAO.remove(3);
//
//        Session session1 = new Session(2, 77, SessionType.VIEWING_SLIDES, new Date(), 77, 77.7);
//        sessionDAO.alter(session1);
//
//        List<Session> sessions = sessionDAO.getAll();
//        sessions.forEach(System.out::println);
//
//        assert sessions.size() > 0;
//
//        assert sessionDAO.getOne(2).getSessionType().equals(SessionType.VIEWING_SLIDES);
    }
}
